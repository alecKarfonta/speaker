apiVersion: v1
kind: Service
metadata:
  name: speaker-frontend-service
  namespace: speaker
  labels:
    app: speaker-frontend
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: speaker-frontend
    component: frontend 