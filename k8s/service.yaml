apiVersion: v1
kind: Service
metadata:
  name: speaker-service
  namespace: speaker
  labels:
    app: speaker
    component: tts-api
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: speaker
    component: tts-api 